# Tone Mosaic Studio

A fully client-side image mosaic tool based on the shared brief. It uploads a source image, samples it into a grid, maps each cell to one of 10 tone tiles, renders the result on canvas, and exports PNG or SVG. The 10 tone tiles can also be replaced with uploaded custom images and reordered by drag and drop.

Open `index.html` in a browser to use it.
